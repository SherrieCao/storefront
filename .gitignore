# secrets
.env

# python
.venv/
__pycache__/
*.pyc

# node / remotion
node_modules/
editor_render/out/
# per-run staged render assets + props (the spike fixtures clip1/moodboard1/voiceover stay)
editor_render/public/[0-9]*/
editor_render/public/_props_*.json

# run outputs
runs/

# read-only old pipeline: kept on disk for reference, not committed (it has its own git
# history + large test media). Reuse PATTERNS from it, don't version a copy.
_reference/

# spike scratch outputs (binary; results captured in docs/*_findings.md)
spikes/out/

# os
.DS_Store

# smoke-test scratch run dirs
_smoke_runs*/

# local test inputs (canonical copies live in _reference/, gitignored)
inputs/
