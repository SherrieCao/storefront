Pillow>=10.0.0
anthropic>=0.40.0
google-genai>=1.0.0
fal-client>=0.5.0
python-dotenv>=1.0.0
requests>=2.31.0
librosa>=0.11.0          # beat detection on the music bed (pipeline/music.py); pulls in numpy
# ffmpeg required system-side for trim / mux / frame extraction (brew install ffmpeg)
# Node 18+ required for the Remotion editor render service (see editor_render/)
